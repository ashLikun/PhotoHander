<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include
        android:id="@+id/done_cancel_bar"
        layout="@layout/crop_layout_done_cancel" />

    <com.ashlikun.photo_hander.crop.CropImageView xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/crop_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/done_cancel_bar"
        android:background="@drawable/crop_texture"
        app:highlightColor="?attr/colorPrimary" />

</RelativeLayout>